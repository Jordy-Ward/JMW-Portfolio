# Cloud PostgreSQL Configuration (Supabase)
# Your actual Supabase database connection details

# Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://db.offuhedyjanoppttmv.supabase.co:5432/postgres}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:kozrip-zUcguq-4romfa}
spring.datasource.driver-class-name=org.postgresql.Driver

# SSL Configuration (required for cloud databases)
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# CORS for production (allows your frontend to connect)
spring.web.cors.allowed-origins=${FRONTEND_URL:http://localhost:3000}